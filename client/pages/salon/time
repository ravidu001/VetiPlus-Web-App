<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Salon Time Slot Management</title>
    <link rel="stylesheet" href="styles.css">
</head>

<style>
            /* General Page Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }

        /* Date Picker Form */
        .date-picker {
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        .date-picker label {
            margin-right: 10px;
            font-size: 16px;
            color: #555;
        }

        .date-picker input[type="date"] {
            padding: 8px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }

        /* Time Slots Table */
        .time-slot-table {
            width: 100%;
            max-width: 600px;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .time-slot-table th, .time-slot-table td {
            padding: 12px;
            text-align: center;
            border: 1px solid #ddd;
        }

        .time-slot-table th {
            background-color: #f0f0f5;
            font-weight: bold;
            color: #333;
        }

        .time-slot-table td {
            font-size: 16px;
        }

        /* Buttons */
        .allow-button, .block-button {
            padding: 6px 12px;
            font-size: 14px;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .allow-button {
            background-color: #4CAF50;
        }

        .allow-button:hover {
            background-color: #45a049;
        }

        .block-button {
            background-color: #f44336;
        }

        .block-button:hover {
            background-color: #d32f2f;
        }

</style>
<body>

<h1>Salon Time Slot Management</h1>

<!-- Date Picker Form -->
<form method="post" action="timeslots.php" class="date-picker">
    <label for="date">Select Date:</label>
    <input type="date" name="date" onchange="this.form.submit()">
</form>

<!-- Time Slots Table -->
<table class="time-slot-table">
    <thead>
        <tr>
            <th>Time Slot</th>
            <th>Status</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <!-- Example rows; replace with PHP code to fetch slots dynamically -->
        <tr>
            <td>08:00 AM</td>
            <td>Allow</td>
            <td>
                <button class="block-button" onclick="updateSlot(1, 'block')">Block</button>
            </td>
        </tr>
        <tr>
            <td>08:30 AM</td>
            <td>Block</td>
            <td>
                <button class="allow-button" onclick="updateSlot(2, 'allow')">Allow</button>
            </td>
        </tr>
        <!-- Additional rows will be dynamically generated by PHP -->
    </tbody>
</table>

<!-- Optional JavaScript for AJAX functionality -->
<script>
function updateSlot(slotId, newStatus) {
    // Placeholder AJAX request; integrate with your backend PHP code
    console.log("Updating slot " + slotId + " to " + newStatus);
    // You would send the slotId and newStatus to the server here
}
</script>

</body>
</html>
